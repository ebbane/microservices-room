class MQTT :
    def__init__(self, topic)

    def add_id(self, player, id):
         if not player.id :
             Player.id = player1_id
         
         if player.id == player.id:
             Player.id = player2_id    