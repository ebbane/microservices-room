class MQTT :
    def__init__(self, topic)

    def add_id(self, player, id):
         if not player :
             player = player1_id
         
         if player == player.id:
             player = player2_id    

    def add_name(self,player, name):
        if not player :
            player = player1_name

        if player == player.name:
            player = player2_name